# Core
spotipy==2.26.0
SQLAlchemy==2.0.49
alembic==1.15.2
psycopg2-binary==2.9.10
python-dotenv==1.2.2

# Download pipeline
# yt-dlp + spotdl unpinned — spotdl constrains yt-dlp internally;
# pinning both causes unsolvable conflicts. Let pip resolve.
spotiflac==0.2.6
yt-dlp[default]
ytmusicapi==1.12.0
spotdl

# Metadata & tagging
mutagen==1.47.0
pyacoustid==1.3.0

# HTTP
requests>=2.25.0

# Daemon
Flask==3.1.3
APScheduler==3.11.0

# UI — version constrained by spotdl
rich

# Dev tools
ruff==0.15.12
mypy==1.20.2
pytest==9.0.3
hypothesis==6.122.3
