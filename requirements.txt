# Core
spotipy==2.26.0
SQLAlchemy==2.0.49
alembic==1.18.4
psycopg2-binary==2.9.12
python-dotenv==1.2.2

# Download pipeline
# yt-dlp + spotdl pinned to the live-resolved versions (P1-3). They coexist at
# these exact versions in the running image, so pinning both IS resolvable (the
# earlier 'unsolvable conflict' note was about pinning to arbitrary versions).
# yt-dlp ships breaking changes often — bump deliberately via Dependabot PRs.
yt-dlp[default]==2026.3.17
ytmusicapi==1.12.0
spotdl==3.9.6
# spotdl 3.9.6 imports pkg_resources (legacy setuptools API).
# setuptools >= 81 (Aug 2025) removed pkg_resources, breaking `import spotdl`
# and the CLI. Pin <81 until spotdl upstream migrates to importlib.metadata.
# Verified: setuptools 80.10.2 still ships pkg_resources.
setuptools<83

# Tier 0: direct Spotify CDN streaming (requires Spotify Premium account)
# PyPI package: https://pypi.org/project/librespot/ (versions 0.0.x)
librespot==0.0.10

# Metadata & tagging
mutagen==1.47.0
pyacoustid==1.3.1
SpotiFLAC==0.7.7

# HTTP & API
fastapi[all]==0.115.11
uvicorn[standard]==0.34.0
websockets==15.0
httpx==0.28.1
pydantic==2.13.4

# Daemon
APScheduler==3.11.2

# UI — version constrained by spotdl
rich==15.0.0

# Dev tools
ruff==0.15.12
mypy==1.20.2
pytest==9.0.3
hypothesis==6.152.4
pytest-cov==7.1.0
