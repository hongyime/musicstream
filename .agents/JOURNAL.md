2026-08-24: Drafted SPEC.md §W3 (discovery parity, quality cutoff mp3_320 default, portability). Decision: -only stack, Plex optional, m3u as portable playlist artifact.
- 2026-08-24 22:13:57 +08:00 [PRAWN-L390/claude/stop] branch=main head=91a0d60 dirty=3
2026-08-25: Implemented SPEC W3a end-to-end. Key decision: m3u exports carry HOST paths (translated from /media) since players run on Windows; token refresher sends client_secret when configured because cached token is confidential-client origin.
- 2026-08-25 00:48:41 +08:00 [PRAWN-L390/claude/stop] branch=main head=91a0d60 dirty=20
- 2026-08-25 07:32:36 +08:00 [PRAWN-L390/claude/stop] branch=main head=91a0d60 dirty=20
- 2026-08-25 09:19:10 +08:00 [PRAWN-L390/claude/stop] branch=main head=4ab24f3 dirty=1
- 2026-08-25 11:11:15 +08:00 [PRAWN-L390/claude/stop] branch=main head=453d4f3 dirty=1
- 2026-08-25 12:05:19 +08:00 [PRAWN-L390/claude/stop] branch=main head=b6d031c dirty=1
2026-09-02: Live stall diagnosis found shallow Docker health was insufficient: daemon was up but APScheduler was not running and startup daemon_run 2509 was incomplete since 2026-08-29. Decision: self-heal must treat /health/deep degraded scheduler/run freshness as daemon repair signal.
2026-09-02: Implemented and live-verified scheduler-first daemon startup plus deep-health watchdog repair. Decision: tests that spawn uvicorn on port 9089 set SKIP_BACKGROUND_STARTUP=true so local test runs do not create live startup/download maintenance rows.
2026-09-02: Added Spotify token refresh at startup/auth status and stale-token refresh before Spotify sync/artist expansion tasks. Decision: scheduled/manual Spotify entry points must repair refreshable cache before constructing Spotipy clients instead of relying only on hourly early-warning probes.
2026-09-02: Added non-blocking Spotify task lock and moved startup Spotify maintenance parallel to download drain. Decision: download progress must not wait behind long Spotify sync/backfill work, and overlapping Spotify triggers should skip instead of piling onto the daemon.
2026-09-02: Added robustness sweep for daemon operations. Decision: manual Spotify controls return job IDs/status instead of blocking, stale DOWNLOADING rows are requeued on a scheduler, deep health includes DB-backed download liveness, and health snapshots are written to JSONL/latest files.
2026-09-02: Hardened Plex self-heal port selection. Decision: automatic PLEX_HOST_PORT rewrites are gated by PLEX_HOST_PORT_AUTO_FALLBACK so conservative installs fail loudly, while the live host can opt into hands-off fallback.
- 2026-09-10: Portfolio review preserves the existing self-hosted pipeline. Recheck runtime placement and measured load before tuning UI polling or connection pools; no provider jobs or backup pruning were triggered.

- 2026-09-16: Baseline review batch-a. No open issues/PRs. Mature codebase (341+ tests), self-hosted, no Vercel/Supabase surface. Deps pinned appropriately. No fixes required — routine heartbeat only.


2026-09-18 — Applied A/B/C/D to musicstream daemon after diagnosing 16h-uptime FastAPI socket stall (RSS 502/512 MiB, endpoint hung, Docker healthcheck too loose to detect). A: Windows scheduled task nightly `docker restart musicstream-daemon` at 04:00. B: compose healthcheck 60s/3→20s/2 with curl -m 5. C: .env MAX_CONCURRENT_WORKERS 8→4 (reverting the 2026-08-25 speed push that wasn't paying off — 17 successes/24h anyway). D: tracemalloc opt-in via TRACEMALLOC_ENABLED=1, hourly dump to logs/tracemalloc.jsonl, gated because Python 3.14 + 10 frames + heavy imports hung the daemon in first attempt. All verified: daemon healthy, endpoint 200, MAX_CONCURRENT=4 in downloader logs.


2026-09-18 (2) — Throughput + memory + leak-hunt second pass. DB shows 45/50 last-24h attempts were tier0_librespot rate_limited; Phase 1 was starving Phase 2. Fixes: LIBRESPOT_SWEEP_CONCURRENT=true (parallel Phase 1/2), LIBRESPOT_SWEEP_MAX_SECONDS=1800 (env-configurable via downloader.py change), download_pipeline cron daily→every 4h. Memory limit 512M→1024M — 4 yt-dlp workers + concurrent Phase 1 blew past 512 MiB in 13 min. Baseline tracemalloc dump moved to asyncio.to_thread (was blocking the health endpoint). Ephemeral pytest run: 329 pass / 3 fail / 4 error — all failures are pre-existing pytest-asyncio plugin loss (not caused by this pass). Neighbours: nebula-sync healthy now, pihole logs report host load 47-60 on 8 vCPU (WSL2 systemic, not musicstream). Committed to main.


2026-09-19 — Leak root-caused: glibc malloc arena bloat, not Python. 5 tracemalloc snapshots over 13h showed RSS +802 MiB while Python-tracked heap flat at 2.5 MiB → native memory. MALLOC_ARENA_MAX unset meant up to 64 arenas (8*nCPU) hoarding freed subprocess memory. Fix: MALLOC_ARENA_MAX=2 + MALLOC_TRIM_THRESHOLD_=100000 in compose env; post-fix RSS 192 MiB stable. SIGUSR1 on-demand dump handler added (threading.Thread, not run_in_executor) but tini doesn't forward SIGUSR1 through gosu exec chain → added tini -g to Dockerfile (needs rebuild). pytest-asyncio==0.26.0 added to requirements.txt (needs rebuild). Rebuild blocked: npm ci hangs >1h under host load 47-60/8cpu. malloc fix + handler code live now via runtime env + bind mount; tini -g + pytest-asyncio pending a quiet-host rebuild.


2026-09-19 (2) — Rebuilt daemon image. Build-stall root cause was CPU starvation from the 29 unifiedcollector/analyzer containers (can't stop them); fixed by temporarily throttling the 6 heaviest to --cpus 0.5 mid-build then restoring to unlimited — build finished, no stop needed. Caught a real bug: pytest-asyncio 0.26 caps pytest<9 vs our pytest==9.1 pin (ResolutionImpossible) — fixed to pytest-asyncio==1.4.0. New image 2026-09-19T08:38Z. Verified: pytest-asyncio 1.4.0 baked in, tini -g in PID1 cmdline, test_daemon_robustness 5 passed (was 3 async failures). Open: 4 test_integration_endpoints errors are env-dependent (spawn real daemon, need DATABASE_URL+live:9079), not regressions; SIGUSR1 dump still doesn't fire (uvicorn likely shadows the loop signal handler) but hourly-cron dump works so it's convenience-only.

2026-09-19 (3) — Continued Kiro's interrupted session and corrected its memory diagnosis: live remote probe measured ~536 MiB tracked Python heap plus ~393 MiB profiler overhead, with an hourly dump inside per-trace filtering. Flat top-15 rows did not prove a native-only leak. Serialized snapshot processing, filtered aggregated statistics, and added sample identity/total-heap/overhead/duration telemetry; retained loop-safe signal dispatch rather than raw signal-thread creation. Endpoint tests now use real ASGI routes with isolated SQLite and HTTPX ASGITransport (old Starlette TestClient is incompatible with pinned HTTPX 0.28); removed the unverified production migration bypass. Local full suite: 351 passed, 1 existing skip. Operational note: delayed Docker exit event required starting the stopped container separately; restored canonical musicstream-daemon name so existing nightly-restart tooling addresses it.

2026-09-19 (4) — Final signal diagnosis found a live tracemalloc-sigusr1 worker plus daemon_logger_disabled=true: signal delivery worked, large snapshots stalled, and Alembic hid diagnostic messages. Preserved loggers, moved blocking health DB work to FastAPI's worker pool, made RSS-only hourly/signal dumps independent of heap profiling, and disabled continuous profiling in local runtime config after one-frame profiling also exceeded the memory target. Real SIGUSR1 produced an untraced 319.0 MiB sample at14:32:44UTC with1.6s processing. Local suite355passed/1skip; Linux sweep's lone new failure was the logging test's60s child-interpreter budget under host pressure, so isolated the real logging configuration in-process instead. HTTP timeouts and the24h memory plateau remain explicitly unproven; no neighbor resources changed.

2026-09-19 (5) — Logging-fixture follow-up passed the affected Linux test (exit0) and the full local suite (355passed/1existing skip); lint and changed-file LSP checks passed. Kept forensic JSON/JSONL logs, removed the temporary probe source and owned QA containers, and preserved pre-existing untracked docker_ports.txt. Record RSS measurements without claiming a24h plateau or fully healthy shared-host HTTP behavior.
- 2026-09-19 23:08:24 +08:00 [PRAWN-L390/claude/stop] branch=main head=0dde9eb dirty=1
2026-09-23 — Rebased and pushed the blocking-route/index fixes from 2026-09-22 onto origin/main after 3 bot-only commits landed there (zero file overlap). Decision: kept team-mode unused for this pass despite being enabled — host was already CPU-saturated (load 82-90 earlier, 34-38 during push) and adding team overhead would worsen contention; proceeded solo with direct tool execution instead. Decision: did not force a fresh container-based pytest re-run before pushing since the rebase was conflict-free and the prior verified run (367 passed, 6 skipped) covered identical file content — Docker's control plane was unresponsive at push time anyway (docker ps/ctr both hung), so live curl checks against the running port substituted for container-based verification. Observed and recorded (not caused by any command here): Docker Desktop/WSL2 self-restarted mid-session under sustained overload; all containers recovered unattended within ~90s. Postgres's CPU pinning (cpuset 0-1) remains runtime-only, not persisted to any compose file — flagged as a gap for a future pass.
2026-09-24 — Closed the postgres cpuset gap flagged 2026-09-23: added `cpuset: "0-1"` to docker-compose.override.yml's postgres block and applied via a full `--force-recreate` (not `docker update`), then confirmed via `docker inspect` that the pin survives a recreate. Decision: paused the fix mid-flight when host load spiked to 108/196/311 (worse than any prior reading) — found 227 recurring orphaned SpiderFoot `sf.py -V` healthcheck children (up from ~61 previously, confirming it's a recurring bug in their design, not ours) and reaped them before proceeding, since recreating a production DB container under that load was unsafe. Decision: did not chase the residual 60-90 load further (node process + unifiedcollector's own DB activity) since that's legitimate neighbor work, not a leak. Found a separate, pre-existing, still-open issue while checking overall health: musicstream-plex unhealthy with its PMS process stuck in D-state on a stale Y:-drive (EXTERNAL_MEDIA_DRIVE) bind mount — same failure class as the 2026-09-22 stale-C:-mount incident, just a different drive, and a plain `docker restart` didn't clear it (unlike a full recreate, restart keeps the same stale mount handle). Decision: did not force a WSL-layer remount or a Docker Desktop restart to fix Plex unilaterally — wsl.exe itself was intermittently hanging on trivial reads at the time (degraded VM state, possibly worsened by the stuck D-state process), and a real fix option (wsl --shutdown) would briefly take down the otherwise-healthy daemon/postgres/scrobbler too; deferred to the user rather than risk broader collateral for a problem outside what was asked.
- 2026-09-24 00:01:05 +08:00 [PRAWN-L390/claude/stop] branch=main head=58887fa dirty=1
