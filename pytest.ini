[pytest]
# Unit tests live in tests/. Keep bare `pytest` from collecting root-level
# manual smoke scripts (e.g. test_ws.py, which needs a live daemon on :9079)
# and the two manual scripts already excluded via tests/conftest.py.
# CI runs `pytest tests/` explicitly; this makes local `pytest` consistent.
testpaths = tests
